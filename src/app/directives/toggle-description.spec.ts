import { ToggleDescription } from './toggle-description';

describe('ToggleDescription', () => {
  it('should create an instance', () => {
    const directive = new ToggleDescription();
    expect(directive).toBeTruthy();
  });
});
